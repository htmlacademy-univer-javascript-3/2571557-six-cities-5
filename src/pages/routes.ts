export enum AppRoutes {
  LOGIN = '/login',
  LOGOUT = '/logout',
  MAIN = '/',
  FAVORITES = '/favorites',
  OFFER = '/offer/:id',
  NOT_FOUND = '/notFound'
}

export enum AuthState {
  AUTH,
  NOT_AUTH,
}
